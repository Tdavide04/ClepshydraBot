# Bytecode
__pycache__/
*.py[cod]

# Virtual environments
.venv/
venv/
env/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Environment
.env

# Python
.pytest_cache/
.mypy_cache/

# Logs
*.log

# SQLite
*.db

# Cache carte Scryfall: dalla migrazione a SQLite (Step 4 roadmap-miglioramenti.md)
# vive nel DB, non serve piu' tracciare il JSON legacy. Puo' restare su disco
# come backup locale/fallback di migrazione: non viene piu' letto ne' scritto
# dal bot una volta popolata la tabella cached_cards.
data/card_cache.json

# Stato "ultimo lastmod visto" per le pagine Event Schedule Arena (utils/arena_event_schedule.py):
# puro bookkeeping operativo per evitare ri-processare pagine gia' notificate, nessun valore
# editoriale da tracciare in git (a differenza di arena_rarity_data.json, che contiene override
# curati manualmente).
data/arena_event_schedule_state.json

# Coverage
.coverage
htmlcov/

# Distribution
build/
dist/
*.egg-info/

# Claude AI config (local)
.claude/